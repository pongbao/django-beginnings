# clone
git clone <git link>

# add files to commit
git add <file>

# commit
git commit -m "<message>"

# send the commit to repository
git push

# update local repository
git pull

# create a new branch
git checkout -b <project name>